<%= simple_form_for(@url) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :shorturl %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
